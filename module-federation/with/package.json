{
  "name": "module-federation-with-example",
  "private": true,
  "scripts": {
    "build": "concurrently \"cd a && pnpm run build\" \"cd b && pnpm run build\" \"cd common && pnpm run build\"",
    "postinstall": "concurrently \"cd a && pnpm install\" \"cd b && pnpm install\" \"cd common && pnpm install\""
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
